{
  "_comment": "Merge these hook entries into your ~/.claude/settings.json. The installer handles this automatically when run with --with-hooks.",
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "~/.claude/hooks/auto-skill-draft.sh"
          }
        ]
      }
    ]
  }
}
